#include <stdio.h>
/**
 * main - Entry point
 * Description: single digit number from base 10
 * Return: Always 0(success)
 */
int main(void)
{
	int n = 48;

	while (n <= 57)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
