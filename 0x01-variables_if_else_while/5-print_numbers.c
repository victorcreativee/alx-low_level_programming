#include <stdio.h>
/**
 * main - A program that prints all singel digit numbers of
 * base 10 starting from 0
 * Return: Always 0(success)
 */
int main(void)
{
	int a = 0;

	while (a < 10)
	{
		printf("%d", a);
		a++;
	}
		printf("\n");

		return (0);
}
